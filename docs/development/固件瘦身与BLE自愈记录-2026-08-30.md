# 固件瘦身记录 — 2026-08-29(lckfb_huangshan_pi / SF32LB52,分支 contest/ai-watch)

## 基线(未改动)

- nuttx.bin:8,192,944 B;flash 8,189,200 B(48.83%);SRAM 416,752 B(79.49%)
- 总 .text:8,217,189 B
- romfs_img:2,759,168 B(libboard.a(romfs_etc.c.o) 内 .rodata)
- 目标库 .text:libgui_wrapper 576,040 / libquickapp 352,231 / liblibqjs 280,636 /
  libfreetype 132,218 / libcurl 285,341 / liblibfeature+impls 150,008 / libuikit 5,968 /
  libpng_static 56,093 / libzlib 20,584 / libyoga 24,923 / libunqlite 35,481 /
  libprotobuf-c 6,443 / liblibcxx 50,385 / libash 27,883
- 入口:CONFIG_INIT_ENTRYPOINT="ai_watch_main";ai_watch 源码零依赖
  QuickApp/QuickJS/UIKIT/FreeType/LVGL demo(仅依赖 LVGL 本体)✅

## 步骤结果

| 步骤 | flash | Δ | SRAM | 说明 |
|---|---|---|---|---|
| 步骤1 删 ROMFS 演示资源 | 5,434,288(32.39%) | -2,758,656 | 不变 | 删 etc/data/app 两个 QuickApp 包 + data/font/MiSans-Regular.ttf(2.59MB);目录结构保留。注意:romfs 打包的是构建树 `boards/exclude_board/src/romfs_etc` 整目录(genromfs -d),需同时清掉构建树内旧拷贝并重跑 cmake(GLOB 在 configure 期收集) |
| 步骤2 关 LVGL demo | 5,294,992(31.56%) | -139,296 | -1,632 | CONFIG_LV_USE_DEMO_WIDGETS / CONFIG_EXAMPLES_LVGLDEMO → not set;map 中 lv_demo_* 符号 0、libapps_lvgldemo 0;liblvgl .text 579,373→443,887 |
| 步骤3 裁 QuickApp/JS 栈 | **2,702,784(16.11%)** | **-2,592,208** | -108,188 | 见下 |

## 步骤 3 分组记录

| 组 | 开关 | 结果 |
|---|---|---|
| QUICKAPP | CONFIG_QUICKAPP | ✅ -1,939,224(prebuilt libgui_wrapper.a+libquickapp.a 整体退出链接;SRAM -80KB) |
| QUICKJS | CONFIG_INTERPRETERS_QUICKJS | ✅ Δ0(libqjs 已随 QuickApp 链被 gc-sections 裁掉) |
| FEATURE_FRAMEWORK | CONFIG_FEATURE_FRAMEWORK | ✅ Δ0(同上,gc) |
| UIKIT | CONFIG_UIKIT | ✅ Δ0(同上,gc) |
| FREETYPE | CONFIG_LIB_FREETYPE + **CONFIG_LV_USE_FREETYPE** | ✅ -147,708;单独关 LIB_FREETYPE 时 LVGL 的 lv_freetype 模块报 ft2build.h 缺失,须同关 |
| YOGA / RAPIDJSON | CONFIG_LIB_YOGA / CONFIG_LIB_RAPIDJSON | ✅ Δ0(gc) |
| PNG | CONFIG_LIB_PNG + **CONFIG_LV_USE_LIBPNG** | ✅ -70,340;单独关 LIB_PNG 时 LVGL lv_libpng 解码器 undefined reference,须同关 |
| CURL/PROTOBUF_C/UNQLITE/LIBCXX | 四组合并一次构建 | ✅(用户同意提速合并);LIBCXX 关闭暴露两个消费者:CONFIG_LIBASH(ASH 运行时,C++,一并关闭)与 h4.c 依赖的 CONFIG_PTHREAD_MUTEX_TYPES(原由 LIBCXX 自动选中,显式钉回) |

## 最终 map 验证

16 个目标库 .text 全部归零(gui_wrapper/quickapp/qjs/freetype/curl/feature×2/uikit/
png/zlib/yoga/unqlite/protobuf-c/libcxx/ash/lvgldemo);ai_watch、aiwatch_ble_bridge、
zblue 均在位;总 .text 2,686,396 B(-5.53MB)。

## 产物

- step1/2/3_nuttx.bin(可分别烧录做分步实机验证)
- step3_group.sh(逐组"关闸→重配→构建"脚本,可复现)

## 待办

- 实机验证:开机 UI/时间/触摸、BLE 连接+校时+提醒横幅、设置页。
- 验证通过后提交:vendor/sifli(21 个资源删除)、contest 仓(defconfig)。

## 追加(08-30):step3 实机问题与修复

| 问题 | 根因 | 修复 |
|---|---|---|
| 上电显示 2036/2/6 6:28 | RTC 硬件保持域内容变化(clock_settime 会回写 RTC,但掉电后寄存器变垃圾值;Year=36 被按 2000+36 解读)。守卫只拦"年<2020",没拦未来垃圾。与瘦身无关 | ai_watch_main.c 增加上限 AI_WATCH_RTC_MAX_YEAR=2035,越界一律显示 --:-- 等校时 |
| 连接后开 Reminder 崩溃(mm_malloc.c:275 堆空闲链表断言) | 崩溃现场 SP=0x20071200 与 36KB init 任务栈底吻合——任务栈耗尽向下越界砸碎相邻堆节点;堆起点随 .bss 缩小移动后,栈的邻居从"宽容分配"变成空闲节点,潜伏问题显形 | CONFIG_INIT_STACKSIZE 36864→65536(SRAM 富余 200KB) |

- 修复后固件:step3b_nuttx.bin(2,702,792B,flash 16.11%,SRAM 58.54% 不变——栈占运行时堆)
- 若 step3b 仍崩溃:下一步回退 LIBCXX+LIBASH 批次再试

## 追加(08-30 #2):BLE 重连崩溃修复

现象:手机两次快速连接(均 0x13 主动断开,未到订阅阶段),第二次连接后 LCPU
控制器报 `Hardware error, hardware code: 0`;主循环 1s 重连循环随即调
`bt_le_adv_stop`,HCI 命令发进已死控制器,10s(HCI_CMD_TIMEOUT)收不到
Command Complete,zblue `bt_hci_cmd_send_sync` 的 `BT_ASSERT_MSG` 直接
kernel oops(fatal.c:49)。

修复:
1. zblue 移植层(external/zblue/zblue,contest/ai-watch 分支):
   - `struct bt_dev` 新增 `sync_sem`(原为 send_sync 调用者栈上的信号量,
     超时返回后迟到的 completion 会 give 悬空栈地址);
   - `bt_hci_cmd_send_sync` 两条路径(syswq/任务上下文)超时改为
     LOG_WRN + unref + 返回 -ETIMEDOUT,不再 oops。
2. 桥接层(sf32lb52_ble_bridge.c):
   - resume 改为"先 start,-EALREADY 才 stop+start"(正常重连路径零多余
     HCI 命令;-ETIMEDOUT 时不做无谓的 stop);
   - 失败后退避 30s,防止每秒一次 × 10s 阻塞把 UI 卡死。

固件:step3c_nuttx.bin(2,703,032B,flash 16.11%,SRAM 58.55%)。

## 追加(08-30 #3):Hardware Error 根因 —— 堆覆盖 BT 邮箱窗口(真凶)

- 芯片 mem_map.h 规定 HCPU SRAM 顶部 1KB(0x2007FC00-0x2007FFFF,2×512B)为
  HCPU↔LCPU 邮箱窗口,通道 1(CH1 @0x2007FE00)承载 BT HCI 流,地址固定。
- 但 sifli_allocateheap.c 的 `up_allocate_heap()` 把堆终点定为 SRAM_END
  (0x20080000)——邮箱窗口整个落在堆里。原固件分配模式恰好没触及顶部;
  瘦身后堆布局变化,分配触顶覆盖邮箱,LCPU 读到损坏的 HCI 流,连接建立时
  报 `Hardware error, hardware code: 0`,服务发现全部超时。
- 修复:堆终点回退 1KB(SRAM_END -= 0x400),邮箱窗口不再可分配。
- 注:上一轮的 zblue -ETIMEDOUT + 桥接退避保留(纵深防御),本轮
  (00:45 日志)中它们已按设计工作:`advertising start failed: -114`
  = -ETIMEDOUT,优雅退避,不再崩溃。

固件:step3d_nuttx.bin(2,703,032B)。堆可用空间仅减少 1KB。

## 追加(08-30 #4):BLE 恢复确认 + 手势/启动体验修复

- 4.3.1 实机确认:彻底断电后 BLE 全链路正常(连接→订阅→校时→提醒推送)。
  此前连续 Hardware error 为 LCPU 跨软复位残留僵死状态所致;邮箱修复有效。
  (heap arena 打印含 PSRAM 8MB,为多区域合计值)
- 4.3.2 修复:
  1. 右滑回主页改为**拖动中触发**(水平位移 >70px 且 |dx|>2|dy| 即刻返回,
     不等松手);松手判定作为慢速滑动兜底,时限 300ms→800ms。
     修复"进入 Reminder/设置后要等一会才能右滑返回"(原 300ms 全程时限
     把从容的滑动静默丢弃)。
  2. 启动加固:/dev/lcd0 等待 5s→10s;LVGL 初始化改为无限重试(原来
     3 次失败直接退出,表现为永久黑屏)。
- 固件:step3f_nuttx.bin(4.3.2)。

## 追加(08-30 #5):Hardware error 仍在 + 诊断升级

- 4.3.2 烧录后连接仍报 Hardware error code 0,且断电后复现——推翻
  "LCPU 残留状态"单一解释;01:17/01:18 两次成功与失败呈随机性,
  指向时序型问题(瘦身后 HCPU 时序变化偶尔落入 LCPU 固件的竞争窗口)。
- 本轮修改:
  1. 堆顶保留区扩大到厂商内存图定义的完整范围 0x500 字节:
     HCPU_CUSTOM_CONFIG 256B(0x2007FB00)+ HPSYS_MBOX_BUF 1KB
     (0x2007FC00);此前只保留了邮箱 1KB,256B 配置区仍暴露给堆。
  2. CONFIG_BT_DEBUG_LOG_LEVEL 6→7:复现时日志将包含控制器死亡前的
     完整 HCI 命令/事件流,用于定位触发点。
- 固件:step3g_nuttx.bin(2,731,340B,DBG 日志 +28KB 为临时诊断开销,
  定位后会关回 INFO 级)。

## 追加(08-30 #6):DBG 日志分析结论 + A/B 对照固件

11:41 DBG 日志分析(连接→死亡全过程):
- 主机侧 HCI 流量 100% 干净:全部命令 status 0x00,ACL 分片仅 9-26 字节
  (远小于控制器 251B 窗口),死亡时无任何主机命令在途;
- 死亡形式:LCPU 控制器**主动**上报 Hardware Error Event(事件码 0x10,
  code 0)——LCPU 蓝牙固件内部致命,发生在手机服务发现的 ACL 交换中;
- 排除项:主机流量、超长分片、RF 校准表(位于控制器私有 RAM
  0x40082000,堆不可达)、LCPU 补丁安装(每次开机执行,不随连接变化)、
  全部厂商共享窗口(step3g 已保护 0x500 字节,死亡依旧)。

结论:现有证据无法把死亡归因于任何 HCPU 侧已知状态;症状呈随机性
(4.3.1 曾连续两次成功)。构建 A/B 对照固件一锤定音:
- fat_dbg_nuttx.bin(5,462,844B):全部库回归 + 今日全部修复 + DBG 日志;
  ROMFS 资源保持删除状态(与 BLE 无关)。
- 判读:胖固件也死 → 与瘦身无关,转向手机端/LCPU 组合;胖固件存活 →
  对瘦身各步二分。

## 收兵(08-30):A/B 判定 + 瘦身范围收缩

- A/B 结果:fat_dbg(全库回归)连续连接稳定 ✅;同日瘦身版随机死亡。
  触发点确定在步骤 2/3 的配置门内(具体组别不再深究)。
- 最终状态(4.4.0,5,434,672B,flash 32.39%,SRAM 79.50%):
  - 保留:步骤 1 ROMFS 资源删减(-2.6MB,A/B 中参与运行已验证无害);
    全部缺陷修复(RTC 年份守卫、init 栈 64KB、SRAM 顶部 1280B 厂商保留区
    防护、zblue send_sync 优雅超时、桥接重连退避、右滑拖动触发、启动无限
    重试、CJK 字体、提醒横幅/生命周期)。
  - 回退:步骤 2(LVGL demo)与步骤 3(QuickApp/QuickJS/UIKIT 等全部
    配置门);BT 日志级别回到 INFO(6)。
- 各历史 bin 保留于本目录备查。若日后重启瘦身,从步骤 2 单独开测。

## 追加(08-30 #7):卡顿排查 —— 性能探针上线(4.4.1)

- "卡"在胖/瘦固件都出现过 → 与瘦身无关,需实测数据定位。
- 按用户要求不加诊断探针,已移除。
- 保留的修复:时间未变时跳过 gmtime_r(原每秒 100 次全量转换,
  主循环 ~100Hz 热路径上的无谓负载)。
- 固件:final_441_nuttx.bin(4.4.1,最终版)。

## 追加(08-30 #8):A/B 结论修正 —— 与瘦身无关,转向触发条件分析

4.4.1(胖配置、DBG 关)也复现 Hardware error → 死亡与瘦身配置门无关,
A/B 的"胖固件正常"是小时样本下的运气(4.3.1 瘦身版也曾连续成功两次)。
故障本质:LCPU 蓝牙固件在连接建立后 ~1s 内偶发内部致命(事件 0x10,
code 0),随机出现,跨胖/瘦固件、跨断电。

新增变量怀疑:手机端 App 的连接模式(深夜日志显示 2-4s 一次的
connect→秒断→重连循环,reason 0x13 均发生在服务发现完成前)。

诊断固件:v442_dbg_nuttx.bin(4.4.1 + DBG=7,同 4.4.0 内容)。
区分实验设计:
1. 用户 App 连接复现 → 抓手表 DBG 日志 + 手机端日志(时间对齐);
2. 换 nRF Connect(或另一台手机)连接多次 → 是否同样死亡;
3. 关掉手机 App 自动重连、手动单次连接 → 是否仍死。

## 追加(08-30 #9):控制器自愈机制上线(4.4.2)

LCPU 蓝牙固件偶发内部致命(闭源,无法修),实现手表侧自愈:

- zblue 移植层:hci_hardware_error 处新增弱钩子 bt_hci_hw_error_notify(),
  RX 线程仅置位不阻塞;
- 桥接层:强符号实现置 hw_error_pending;新增 ai_watch_ble_bsp_recover():
  1) sf32lb52_bt_controller_deinit() —— LCPU 断电、适配器回 IDLE;
  2) bt_disable_mc(0) —— 主机拆解;其 HCI_RESET 经适配器自动恢复路径
     重启全新 LCPU(补丁+RF 校准),因此拆解干净完成(hdev 释放);
  3) ai_watch_ble_bsp_start() —— bt_enable 全新初始化 → 重注册 GATT →
     重新广播;手机重连循环自动接续。带 10s 频率限制,恢复期间 UI 阻塞
     约 3-5s。
- 应用主循环:ai_watch_ble_process() 轮询 take_hw_error() 触发恢复。
- 符号验证:nuttx.map 确认强符号(bridge)覆盖弱默认。
- 固件:v442_selfheal_nuttx.bin(4.4.2,DBG 暂开用于验证自愈,稳定后关回)。

## 追加(08-30 #10):自愈未触发的根因 + 修复(4.4.3)

- 现象:hw error 后无 "recovering" 日志,恢复未执行。
- 根因:ELF 弱符号陷阱——弱定义与调用点同在 hci_core.c 编译单元时,
  编译器将调用直接绑定到本地弱实现,桥接层的强定义不参与解析。
- 修复:改为注册回调模式(hci_core 存函数指针 + 新增
  bt_hci_hw_error_cb_register(),声明进 bluetooth.h;桥接在
  bsp_start() 注册 bridge_hw_error_notify)。注册模式与移植层其他
  回调一致,且无链接期绑定陷阱。注册在 hdev 生命周期之外,自愈重建后仍有效。
- 固件:v443_selfheal_nuttx.bin(4.4.3,DBG 保持开启验证自愈)。

## 追加(08-30 #11):自愈触发修复(4.4.3)

- 弱符号改为注册回调(bt_hci_hw_error_cb_register,声明进 bluetooth.h;
  桥接在 bsp_start 注册)。注意两个工程坑:
  1) 构建树的 zblue 头文件是 configure 期 file(COPY) 的副本,改源码
     头文件后必须重跑 cmake configure 才生效;
  2) 前向声明必须位于使用点(bsp_start)之前。
- 固件:v443_selfheal_nuttx.bin(4.4.3,DBG 开)。
